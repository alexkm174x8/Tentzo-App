import SwiftUI

struct CameraView: View {
    var body: some View {
        Camera()
    }
}

struct CameraView_Previews: PreviewProvider {
    static var previews: some View {
        CameraView()
    }
}

